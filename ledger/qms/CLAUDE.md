# ledger/qms/ — QMS Data Rules

Quality Management System records.

## Contents
- `process-map.json` — ISO 9001 SIPOC (machine-readable)
- Future: risk snapshots, management review minutes

## Rules
- All files are system-maintained — do NOT edit manually
- Changes require change-request Issue + dual approval PR
- JSON files must pass schema validation before merge
- `process-map.json` must conform to `core/schemas/process-map.schema.json`
